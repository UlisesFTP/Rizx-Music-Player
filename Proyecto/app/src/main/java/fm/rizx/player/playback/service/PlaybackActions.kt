package fm.rizx.player.playback.service

/**
 * The session's custom commands, shared with every trusted controller in this package — the media
 * notification's buttons and the home screen widgets alike.
 */
object PlaybackActions {
    const val TOGGLE_FAVORITE = "fm.rizx.player.action.TOGGLE_FAVORITE"
    const val CYCLE_REPEAT = "fm.rizx.player.action.CYCLE_REPEAT"
}
